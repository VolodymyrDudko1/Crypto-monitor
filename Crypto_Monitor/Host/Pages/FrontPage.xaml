<Page x:Class="Crypto_Monitor.Host.Pages.FrontPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="700"
      d:DesignWidth="1000"
      Title="FrontPage"
      Loaded="Page_Loaded"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Crypto Monitor" FontSize="36" FontWeight="Bold"/>
            <TextBlock Text="Simple host frontpage for microservice phase 1" Opacity="0.8" Margin="0,6,0,0"/>
        </StackPanel>

        <Border Grid.Row="2"
                CornerRadius="12"
                Padding="16"
                Background="{DynamicResource SystemControlAcrylicWindowBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="MarketData API Status" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="ApiStatusText" Text="Checking..." Margin="0,6,0,0"/>
                </StackPanel>

                <Border Grid.Column="1"
                        Padding="10,4"
                        CornerRadius="10"
                        VerticalAlignment="Center"
                        Background="#1F22C55E">
                    <TextBlock Text="PHASE 1" Foreground="#22C55E" FontWeight="Bold"/>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border CornerRadius="12" Padding="16" Background="{DynamicResource SubtleFillColorSecondaryBrush}">
                <StackPanel>
                    <TextBlock Text="Quick Actions" FontSize="18" FontWeight="SemiBold"/>
                    <Button x:Name="OpenOverviewButton" Content="Open Market Overview" Margin="0,12,0,0" Height="36" Click="OpenOverviewButton_Click"/>
                    <Button x:Name="RefreshDataButton" Content="Refresh Data" Margin="0,8,0,0" Height="36" Click="RefreshDataButton_Click"/>
                    <TextBlock x:Name="LastUpdatedText" Margin="0,12,0,0" Opacity="0.8" Text="Last updated: -"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" CornerRadius="12" Padding="16" Background="{DynamicResource SubtleFillColorSecondaryBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Top Coins (Live)" FontSize="18" FontWeight="SemiBold"/>

                    <DataGrid x:Name="CoinsGrid"
                              Grid.Row="2"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding CoinName}" Width="*"/>
                            <DataGridTextColumn Header="Ticker" Binding="{Binding Ticker}" Width="90"/>
                            <DataGridTextColumn Header="Price (USD)" Binding="{Binding Price, StringFormat=N2}" Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
