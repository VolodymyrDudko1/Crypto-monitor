<Page x:Class="Crypto_Monitor.Host.Pages.CoinInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="700"
      d:DesignWidth="1000"
      Loaded="Page_Loaded"
      Title="CoinInfoPage"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="← Back" Width="90" Height="32" Click="BackButton_Click"/>
            <TextBlock x:Name="TitleText" Margin="16,0,0,0" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" CornerRadius="12" Padding="16" Background="{DynamicResource SubtleFillColorSecondaryBrush}">
            <StackPanel>
                <TextBlock x:Name="TickerText" FontSize="18" FontWeight="SemiBold"/>
                <TextBlock x:Name="PriceText" Margin="0,10,0,0"/>
                <TextBlock x:Name="MarketCapText" Margin="0,6,0,0"/>
                <TextBlock x:Name="ChangeText" Margin="0,6,0,0"/>
                <TextBlock x:Name="SourceText" Margin="0,10,0,0" Opacity="0.8"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
